{
  "homepage": "https://Sagnik-Bankura.github.io/expense-tracker",
  "scripts": {
  "start": "react-scripts start",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build",
  "build": "react-scripts build",
  "test": "react-scripts test",
  "eject": "react-scripts eject"
},
  "dependencies": {
    "apply": "^0.2.19",
    "git": "^0.1.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
